
#ifndef HEADER_H   // include guard (prevents multiple inclusion errors)
#define HEADER_H

// Function prototypes with meaningful names
int power(int base, int exponent);
int powerV2(int base, int exponent);

#endif
